services:
  post-studio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: post-studio-app
    ports:
      - "${APP_PORT:-8080}:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
